<%= form_with(model: lockercliente) do |form| %>
  <% if lockercliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lockercliente.errors.count, "error") %> prohibited this lockercliente from being saved:</h2>

      <ul>
        <% lockercliente.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome_empresa %>
    <%= form.text_field :nome_empresa %>
  </div>

  <div class="field">
    <%= form.label :site %>
    <%= form.text_field :site %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
